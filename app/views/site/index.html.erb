
  <p>
    <!-- <a href="/signup" class="btn btn-primary">Please Sign Up</a> -->
    <!-- refactored with rails helpers: -->
    <%= link_to "Sign Up", signup_path, class: "btn btn-primary" %>
    &nbsp; or &nbsp;
    <%= link_to "Log In", login_path, class: "btn btn-primary" %>
  </p>
<div class="col-sm-4 col-sm-offset-4">
<%= form_for :post, url: "/posts", method: "post" do |f| %>
  <%= f.text_field :title %>
  <%= f.submit "save post", class: "btn btn-primary" %>
  <% end %>
</div>
<br>
<div class="container">
  <%= form_tag posts_path, { method: "get", :class => 'navbar-form', :role => 'search'} do %>
  <div class="input-group input-group-lg">
    <%= text_field_tag :search, params[:search], placeholder: "search for the best", class: 'form-control', required: true %>
    <div class="input-group-btn">
      <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
    </div>
    <% end %>
</div>